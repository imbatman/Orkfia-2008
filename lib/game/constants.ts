/**
 * Legacy constants placeholder
 *
 * TODO: Port values from _php/constants.php
 */

export const GAME_CONSTANTS = {
  // Example placeholders; replace with real values during port
  TICK_HOURS_PER_DAY: 24,
  STARTING_GOLD: 1000,
};

export enum Role {
  USER = "user",
  RESORT_HEAD = "resort_head",
  STAFF = "staff",
  ADMIN = "admin",
}
