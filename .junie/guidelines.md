Always use shadcn/ui for primitives. If no primitive exists and it makes sense to create a shared one, create it.
Always use `steps.md` for persisting tasks/actions/steps and their progress.
Always use `context.md` for the context of this project.